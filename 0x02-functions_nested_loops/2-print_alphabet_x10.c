#include<stdio.h>
/**
 * main - check the code
 * Return: always 0
 */
void print_alphabet_x10(void)
{
	int coun = 0;
	char letter;

	while (count++ <=9)
	{
		for (letter = 'a'; letter <='z'; letter++)
			_putchar(letter);
		_putchar('\n);
	}
}
